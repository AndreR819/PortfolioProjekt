[project]
name = "portfolioprojekt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AndreR", email = "retlife@web.de" }
]
requires-python = ">=3.14"
dependencies = [
    "certifi>=2026.7.22",
    "ipykernel>=7.3.0",
    "lxml>=6.1.2",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]

[project.scripts]
portfolioprojekt = "portfolioprojekt:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
